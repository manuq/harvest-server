CREATE TABLE laptops (
    serial_number VARCHAR(255),
    uuid VARCHAR(255),
    model VARCHAR(255),
    update_version VARCHAR(255),
    build VARCHAR(255),
    updated INT(11),
    collected INT(11),
    stored INT(11),
    PRIMARY KEY (serial_number)
);
